# Security configurations
audit-level=moderate
fund=false

# Performance optimizations  
registry=https://registry.npmjs.org/
cache-min=10

# Prevent automatic package-lock.json updates for security
package-lock=true

# Security audit on install
audit=true